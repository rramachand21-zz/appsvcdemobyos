Upload the contents of wwwroot_upload_to_storage.zip to an Azure Blob storage or Azure Files share.

And then you can deploy this web app to an App Service which is linked to the Azure Blob storage or Azure Files share.

Contents of the wwwroot directory will be mounted from Azure Storage to serve the images, css and js files for this webapp.
